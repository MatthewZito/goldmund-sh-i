version: '3.8'

services:
    goldmund-api:
        container_name: goldmund-api
        restart: always
        build: ./
        command: npm start
        working_dir: /usr/src/dps-api
        ports:
            - "5000:5000"
        volumes: 
            - ./:/usr/src/dps-api









            
    #     links: 
    #         - mongo
    # mongo:
    #     container_name: mongod
    #     image: mongo
    #     volumes:
    #         - ~/opt/mongodb-data:/data/db
    #     ports:
    #         - "27017:27017"